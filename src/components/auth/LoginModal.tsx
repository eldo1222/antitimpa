import React from 'react';
import { GoogleAuthModal } from './GoogleAuthModal';

export const LoginModal: React.FC = () => {
  // Pure Google OAuth Authentication is used across the entire application
  return null;
};

